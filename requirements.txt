# Use these versions that have pre-compiled wheels for Windows
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
numpy==1.24.3  # Older version with pre-compiled wheels
pandas==2.0.3  # Older version with pre-compiled wheels
joblib==1.3.2
python-multipart==0.0.6